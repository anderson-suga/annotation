nano ~/scripts/gitconfig.sh

git config --local user.name $1
git config --local user.email $2

chmod +x ~/scripts/gitconfig.sh

nano ~/.bashrc

export PATH=$PATH:~/scripts
export USER_TOKEN="https://<user>:<token>@"

source ~/.bashrc


# gitignore local
.git/info/exclude
